import { Routes } from '@angular/router';
import { RequestsPermissionComponent } from './requests-permission.component';

export const ROUTES: Routes = [
  { path: '', component: RequestsPermissionComponent },
];
